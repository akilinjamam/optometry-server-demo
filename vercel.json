{
    "version": 2,
    "name": "optometrist-demo-server",
    "builds": [
       { "src": "server.ts", "use": "@vercel/node" }
    ],
    "routes": [
       { "src": "/(.*)", "dest": "/server.ts" }
    ]
 }